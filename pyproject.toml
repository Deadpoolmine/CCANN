[build-system]
requires = ["setuptools>=64", "wheel", "cmake>=3.18; sys_platform != 'win32'", "pybind11>=2.10; sys_platform != 'win32'", "ninja; sys_platform != 'win32'"]
build-backend = "setuptools.build_meta"
